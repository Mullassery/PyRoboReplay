# Sensitive files and credentials
.env
.env.local
.env.*.local
*.key
*.pem
*.crt
credentials.json
secrets.json
.credentials
config.local.*
.config.local
oauth_tokens.json

# IDE files
.vscode/
.idea/
*.swp
*~
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# Virtual environments
venv/
env/
.venv

# Rust
target/
# Cargo.lock is intentionally committed: this crate ships a real binary
# (`pyroboreplay`, src/main.rs) in addition to the library, so the lockfile
# is kept for reproducible builds (see ROADMAP_HONEST.md technical debt notes).

# Temp files
*.tmp
*.log
temp/
tmp/
